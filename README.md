# clone後 アプリ起動操作

### yarn版
#### `yarn install`
↓
#### `yarn add axios`
↓モックサーバー起動
#### `npx json-server --watch db.json --port 3100`
↓
#### `yarn start`
##

### npm版
#### `npm install axios`
↓モックサーバー起動
#### `npx json-server --watch db.json --port 3100`
↓
#### `npm start`
